<div class="vizamyl_logo">
  <% if I18n.locale == :'en-us' && session[:origin] == "us" %>
    <%= image_tag('vizamyl-logo.png') %>
  <% else %>
  
  <div class="vizamyl_logo">
    <div class="logo_text">
      <p class="global_brand">
        <%= link_to t('layouts.application.logo.1_html'), "/#{I18n.locale}" %>
        <%= t('layouts.application.logo.2_html') %>
        <%= t('layouts.application.logo.3_html') %>
      </p>
    </div>
  <% end %>

  <!-- <div class="burger_menu d-xl-none d-block">
    <a href="#"><i class="fa-solid fa-bars"></i></a>
  </div> -->
</div>
